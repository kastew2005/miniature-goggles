import THREE from "../three.js";
export class Weather{
 constructor(scene,quality={}){this.scene=scene;this.quality=quality;this.rainCount=Math.min(quality.rain||120,160);this.rain=new THREE.LineSegments(this.makeRainGeometry(),new THREE.LineBasicMaterial({color:0x9fc8ff,transparent:true,opacity:.28}));this.rain.visible=false;this.rain.frustumCulled=false;scene.add(this.rain);this.flash=0;this.clouds=this.makeClouds();scene.add(this.clouds)}
 makeRainGeometry(){const a=new Float32Array(this.rainCount*6);for(let i=0;i<this.rainCount;i++){const j=i*6,x=(Math.random()-.5)*42,z=(Math.random()-.5)*42,y=Math.random()*20;a[j]=x;a[j+1]=y;a[j+2]=z;a[j+3]=x+.07;a[j+4]=y-1.3;a[j+5]=z}const g=new THREE.BufferGeometry();g.setAttribute('position',new THREE.BufferAttribute(a,3));return g}
 makeClouds(){const g=new THREE.Group(),count=Math.min(this.quality.clouds||12,12),geo=new THREE.BoxGeometry(1,1,1),mat=new THREE.MeshBasicMaterial({color:0xffffff,transparent:true,opacity:.08});for(let i=0;i<count;i++){const m=new THREE.Mesh(geo,mat);m.position.set((Math.random()-.5)*180,75+Math.random()*18,(Math.random()-.5)*180);m.scale.set(7+Math.random()*9,1+Math.random()*1.5,4+Math.random()*6);g.add(m)}g.frustumCulled=false;return g}
 update(dt,player,time){const storm=Math.sin(time*.11)>.78||Math.sin(time*.037+2)>.92;this.rain.visible=storm;this.rain.position.set(player.pos.x,player.pos.y+12,player.pos.z);if(storm){const p=this.rain.geometry.attributes.position.array;for(let i=0;i<this.rainCount;i++){const j=i*6;p[j+1]-=dt*22;p[j+4]-=dt*22;if(p[j+1]<0){const y=20+Math.random()*4;p[j+1]=y;p[j+4]=y-1.3}}this.rain.geometry.attributes.position.needsUpdate=true}this.clouds.position.x=player.pos.x*.02+(time*.35%120)-60;this.clouds.position.z=player.pos.z*.01;if(storm&&Math.random()<dt*.02)this.flash=1;this.flash=Math.max(0,this.flash-dt*4);document.body.style.setProperty('--lightning',String(this.flash))}
}
